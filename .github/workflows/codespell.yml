name: codespell

on:
  push:
    branches:
      - mc
  pull_request:
    branches:
      - mc

jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: codespell-project/actions-codespell@v2.2
        with:
          skip: ./.git,./go.sum
